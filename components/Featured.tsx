import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Featured({}: Props): ReactElement {
    return (
        <div id='featured'>
           featured stuff 
        </div>
    )
}