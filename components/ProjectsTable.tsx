import React, { ReactElement } from 'react'

interface Props {
    
}

export default function ProjectsTable({}: Props): ReactElement {
    return (
        <div>
           projectstable 
        </div>
    )
}
